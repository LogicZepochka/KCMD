target = get_custom_object.target
result = {
    type: "none", // - Тип для обработчика команды external. Если none, то мы ничего не обрабатываем. targets - список эксплойтов (например, если ломанули изнутри root shell)
    object: null
}
// ---CODE HERE



// ---END CODE
// Если нужно что-то вернуть обратно (например, новые эксплойты)
get_custom_object.externalResult = result